{
  "name": "satquery-ai",
  "version": "1.0.0",
  "description": "SatQuery AI — Agentic Vision-Language Assistant for Remote-Sensing Imagery (SIH 2026)",
  "private": true,
  "scripts": {
    "dev:ml": "cd ml-service && venv\\Scripts\\uvicorn app.main:app --reload --reload-dir app --port 8000 --env-file .env",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:ml\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}


